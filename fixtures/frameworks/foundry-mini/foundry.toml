# Minimal Foundry project fixture for velvet adapter tests (original work).
[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.24"
remappings = ["minilib/=lib/minilib/src/"]
